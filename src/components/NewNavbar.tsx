// Re-export do Navbar geral. O Layout (App.tsx) usa <NewNavbar /> em todas
// as páginas, e este é o cabeçalho global (azul, com sino e cesto a
// funcionar) que inspira o design pedido pelo utilizador.
export { default } from "./Navbar";
